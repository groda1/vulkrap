
pub fn alloc() {

  //  use std::alloc::{alloc, Layout};
  //  let layout = Layout::new::<[u32; 2_000_000]>();
   // let mut buf = unsafe {
    //    let ptr = alloc(layout) as *mut [u32; 2_000_000];
     //   Box::from_raw(ptr)
    //};


}

